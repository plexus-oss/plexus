# Compiled binary
gateway

# Local env (copy .env.example → .env to bootstrap)
.env
*.env.local

# Production secrets for deploy.sh — never commit
.env.deploy

# Local Redis volume (docker-compose)
data/

# claude
memory

# extras
__pycache__
